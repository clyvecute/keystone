version: 2
updates:
  # Keep GitHub Actions versions up to date
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"  # weekly was too noisy
    groups:
      github-actions:
        patterns:
          - "*"
