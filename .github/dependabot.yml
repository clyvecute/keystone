version: 2
updates:
  # Keep GitHub Actions versions up to date (minor/patch only — no major bumps)
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    groups:
      github-actions:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"
